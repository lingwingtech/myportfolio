# Prompt — Build a new page

在当前 Personal Web Harness 中新增一个页面。

要求：
1. 先阅读 `AGENTS.md` 与 `HARNESS.md`。
2. 优先复用 `Container`、`SectionTitle` 和现有排版系统。
3. 新页面添加到 React Router。
4. 页面必须调用 `useDocumentTitle()`。
5. 不新增 UI 框架。
6. 保证 375 / 768 / 1440px 响应式布局。
7. 完成后运行 typecheck、lint、test、build。
